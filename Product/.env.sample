# OpenAI
OPENAI_API_KEY=sk-...

# LMStudio (only needed if using lmstudio provider)
LMSTUDIO_BASE_URL=http://host.docker.internal:1234/v1

# PostgreSQL (docker compose defaults)
DATABASE_URL=postgresql://graphrag:graphrag@postgres:5432/graphrag

# Local document storage base path (inside the container)
STORAGE_BASE=/docs
